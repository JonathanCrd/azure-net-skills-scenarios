<!--
  TEMPLATE: Copy this entire _template/ directory to create a new scenario.
  
  Example:
    cp -r scenarios/_template scenarios/my-new-scenario

  Then replace this file's contents with your plain-language prompt.
  
  Guidelines:
  - Write as if you're typing into Copilot Chat or Claude — a plain, self-contained request.
  - Ask for a small .NET 8 console application with 2-4 C# classes.
  - Be specific about class names, methods, and what each class should do.
  - Specify which Azure services to use and how to authenticate (DefaultAzureCredential).
  - Mention .NET version and any key NuGet dependencies.
  - Don't include evaluation criteria in the prompt — that lives in the evaluator's head
    and in docs/evaluation-guide.md.
-->

Write your prompt here. Delete these instructions when done.
